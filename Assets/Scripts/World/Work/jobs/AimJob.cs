using Commons;
using Foundations;
using World.Helpers;

namespace World.Work.jobs
{
    public class AimJob:Job
    {
        public override void tick()
        {
           /* base.tick();
            Mission.instance.try_get_mgr(Config.WorkMgr_Name, out WorkMgr wmgr);
            var slot_name = Device_Slot_Helper.GetSlot(owner);
            wmgr.workPanelMgr.UpdatePanel(slot_name, "AimPanel", IsWorking(), owner);*/
        }

    }
}
